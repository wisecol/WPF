<UserControl x:Class="training.UserControlCustomers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:training"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Column="0" Background="#FFD6CF2B" VerticalAlignment="Stretch" CornerRadius="15" Margin="5">
            
        </Border>

        <Border Grid.Column="1" Background="#FF2BD642" VerticalAlignment="Stretch" CornerRadius="15" Margin="5">

        </Border>

        <Border Grid.Column="2" Background="#FF3E4299" VerticalAlignment="Stretch" CornerRadius="15" Margin="5">

        </Border>

        <Border Grid.Column="3" Background="#FFB649C7" VerticalAlignment="Stretch" CornerRadius="15" Margin="5">

        </Border>

    </Grid>
</UserControl>
